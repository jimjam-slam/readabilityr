can_read <- function (document, min_content_length = 140, min_score = 20) {

    # An R function that wraps around the isProbablyReaderable function

}

parse_site <- function () {

    # An R function that parses the website

}